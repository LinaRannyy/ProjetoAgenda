<nav class="navbar navbar-expand-lg navbar-light" style="background-color: var(--card-bg); box-shadow: var(--box-shadow);">
    <div class="container">
        <a class="navbar-brand" href="/" style="color: var(--primary-color); font-weight: 600;">Agenda</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
    
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ml-auto">
                <% if(user) { %>
                <li class="nav-item">
                    <a class="nav-link" href="/contatos/index">Cadastrar contato</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="/login/logout">Sair</a>
                </li>
                <% } else { %>
                <li class="nav-item">
                    <a class="nav-link" href="/login/index">Entrar</a>
                </li>
                <% } %>
            </ul>
        </div>
    </div>
</nav>